extends layout

block content
  h1= title
  a(href="/new")= "Add your own message"
  ul
    each message in messages
      div
        li= message.text 
        li= message.user 
        li= message.date